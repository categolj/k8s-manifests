apiVersion: secretgen.carvel.dev/v1alpha1
kind: SecretExport
metadata:
  name: registry-credentials
  #! registry-credentials is used in build-service
  namespace: tap-install
spec:
  toNamespaces:
  - '*'