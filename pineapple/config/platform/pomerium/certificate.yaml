apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pomerium
  namespace: pomerium
spec:
  dnsNames:
  - "*.pomerium.pineapple.maki.lol"
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-maki-lol
  secretName: pomerium-wildcard-tls