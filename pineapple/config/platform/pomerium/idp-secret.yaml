#@ load("@ytt:data", "data")
apiVersion: v1
kind: Secret
metadata:
  name: idp
  namespace: pomerium
type: Opaque
stringData:
  client_id: #@ data.values.client_id
  client_secret: #@ data.values.client_secret